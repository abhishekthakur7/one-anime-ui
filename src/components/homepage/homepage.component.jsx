import React from 'react';
import MainContainer from '../container/main-container.component';

export default function HomePage () {
    return (
        <div>
            <MainContainer />
        </div>
    );
}